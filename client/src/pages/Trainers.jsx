import React,{useEffect,useState} from "react"; import api from "../api";
export default function Trainers(){const [items,setItems]=useState([]);useEffect(()=>{api.get("/trainer").then(r=>setItems(r.data))},[]);return <main className="container py-4"><h2>Trainer Management</h2><div className="row g-3">{items.map(t=><div className="col-md-6 col-lg-4" key={t._id}><div className="card p-4"><h5>{t.user?.name}</h5><p className="text-primary">{t.specialization}</p><p>{t.user?.bio||"Fitness professional available for community support."}</p><div>Experience: {t.experienceYears} years</div><div>Availability: {t.availability}</div></div></div>)}{!items.length&&<p>No trainers registered yet.</p>}</div></main>}
